import { PackageStop } from "../types";

/**
 * PAQUETES DEL DÍA para el tramo Moléson 1 → Montsalvens 1 (de tu ruta real).
 *
 * 'routeStopId' referencia el 'id' de la parada en testRoute.ts.
 * Los 'packageCount' son aproximados (el vídeo mostraba el icono de paquete,
 * no el número exacto): 1 = un sobre/paquete, 2 = varios. Edítalos si quieres.
 */
export const TEST_PACKAGES: PackageStop[] = [];
